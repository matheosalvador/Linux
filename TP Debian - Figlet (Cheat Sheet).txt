1) Une fois votre machine virtuelle « debian » correctement connectée au réseau, quelles sont les
deux commandes à effectuer l’une à la suite de l’autre afin de mettre à jour le système ? Exécutez
ces commandes.

sudo apt update
sudo apt upgrade

2) Quelle commande utilisant « dpkg » permet de savoir si le (génial) paquet « figlet » est
installé sur votre VM debian?

dpkg -l package_name </ dpkg -l figlet />

3) Installer le paquet « figlet » … sans oublier les commandes indispensables à effectuer avant
toute installation.

sudo apt install package_name </sudo apt install figlet/>

4) Quelle commande permet de connaître la version installée de « figlet » ?

package_name -v </figlet -v/> 

5) Quelle commande permet d’afficher le mot « Welcome »avec « figlet » ?

figlet welcome

6) Quelle commande permet d’afficher de la droite vers la gauche le mot « Welcome » avec
« figlet » ?

figlet -r 

7) Utilisez les fontes « banner », « lean », « shadow », « script » et « slant »
afin d’afficher un message de votre choix.

figlet -f font_name </figler -f banner, lean, shadow, script, slant

8) Quelle option de « figlet » permet de centrer un message ?

figlet -c 

9) Quelle option de « figlet » permet de préciser que le message a 40 colonnes de largeur ?

figlet -w weight_number

10) En utilisant le protocole SSH, tranférez le fichier fourni « The Universal
Declaration of Human Rights.txt » dans le home directory de votre utilisateur.

sudo apt install tree
pwd </savoir où l'on est/>
ls </voir le nom du dossier/>
tree </marche pas/>
ls -la </affiche les fichier cachés/>
mkdir folder_name </créer un dossier/>
cd "path" </se déplacer de dossiers en dossiers et fichiers en fichiers/>
echo "Contenu de UN" > un </crée un fichier "un" met dans ce fichier un texte "Contenu de UN"/>
more file_name </permet de lire le contenu d'un fichier par exemple "un"/>
mv </permet de déplacer un fichier dans un dossier permet aussi de renommer un fichier/>
cp file_path copy_path </copier un fichier vers un emplacement/>
cp -r file_path copy_path </copier un dossier et son contenu vers un emplacement/>
rmdir -r </supprimer un dossier et son contenu/>
touch </changer la date de modif d'un dossier

cd my-mess
sudo mv home/bobby/my-mess/The\ Universal\ Declaration\ of
\ Human\ Rights.txt home

11) Quelle commande « figlet » permet de transformer tout le texte contenu dans le fichier
« The Universal Declaration of Human Rights.txt » et de stocker le résultat
dans un fichier nommé « human-rights.txt » ?

sudo sh -c 'figlet < "The Universal Declaration of Human Rights.txt" > human-rights.txt'

13 ) Arrêtez proprement votre machine fonctionnant sous Debian

sudo shutdown -h nom

Autor: Mathéo Salvador

Commandes :

sudo su 
passwd username </ permet de changer le mot de passe d'un utilisateur />
ssh <user name>@<ip> </ permet de se connecter à sa vm via son windows />
ip addr </ permet d'identifier son Adresse Ip />
sudo hostnamectl set-hostname nouveau_hostname </ permet de changer l'hostname de sa vm />
mkdir -p DossierNiveau1/Niveau2/Niveau3/Niveau4.a </ commande pour créer un répertoire simple et une arborescence complexe />